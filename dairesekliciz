
            int centerX = getWidth() / 4;
            int centerY = getHeight() / 4;

            // Yarıçapı hesapla
            int radius = Math.min(centerX, centerY);

            // Her bir çocuğu (TextView) daire üzerinde konumlandır
            for (int i = 0; i < letter.length; i++) {
                //  View child = getChildAt(i);



                // Açıyı hesapla
                double angle = 2 * Math.PI * i / 4;

                // Çocuğun pozisyonunu hesapla
                int childX = (int) (centerX + radius * Math.cos(angle));
                int childY = (int) (centerY + radius * Math.sin(angle));

                TextView tvi=new TextView(getContext());
                tvi.setText(letter[i]);
                tvi.setTextSize(80);
                linearLayout= new LinearLayout.LayoutParams(80,80);
               /tvi.setX(childX);
                / tvi.setY(childY);
                tv.add(tvi);
                canvas.d
               // addContentView(tvi,linearLayout);
                // Çocuğu yerleştir
                // child.layout(childX, childY, childX + child.getMeasuredWidth(), childY + child.getMeasuredHeight());
            }
